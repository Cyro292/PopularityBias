# Retrieval Gold-Document Evaluation

These legacy notebooks define retrieval correctness as returning a chunk from
the annotated Wikipedia document. They are retained separately from
`notebooks/retrieval_answer_eval`, which scores whether retrieved chunk text
contains a gold answer alias.
